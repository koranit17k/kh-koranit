ขั้นตอนการ copy ไฟล์จาก dist(local) ไปยัง web server(/var/www/dist)
สร้างไฟล์ดิส
```bash
pnpm generate
```
เข้าโฟลเดอร์
```bash
cd kh-koranit
```

copy ไฟล์จาก dist(local) ไปยัง web server(/var/www/dist)
```bash
sudo rsync -av --delete dist/ /var/www/dist/
```